# FlowForge Web UI Environment Configuration
# Copy this file to .env and update the values

# ===========================================
# API Host Configuration
# ===========================================
# The hostname or IP where FlowForge services are running
# Use 'localhost' for local development
# Use server IP (e.g., 10.0.0.115) for remote Docker host
VITE_API_HOST=localhost

# Kong Gateway URL (main API endpoint)
VITE_API_URL=http://localhost:8000

# Kong Admin URL (for service management - optional)
VITE_KONG_ADMIN_URL=http://localhost:8001
