# ===========================================
# FlowForge Development Environment
# ===========================================

# Backend API URL for Vite dev server proxy (internal)
VITE_API_URL=http://10.0.0.115:4000

# Browser-side API base URL (Kong gateway)
# If not set, uses current hostname with port 8000
# VITE_API_BASE_URL=http://10.0.0.115:8000

# Override just the port for browser API calls (default: 8000)
# VITE_API_PORT=8000
