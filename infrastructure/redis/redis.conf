# Redis Configuration for FlowForge

# Memory
maxmemory 256mb
maxmemory-policy allkeys-lru

# Persistence (disabled for cache-only mode)
save ""
appendonly no

# Network
bind 0.0.0.0
protected-mode no
port 6379

# Logging
loglevel notice
